package program

import java.util.ArrayList;
import java.util.List;
import DAL.SerializeHelper;
import form.MainForm;
public class Program {

	@SuppressWarnings("unchecked")
	public static void main(String[] args) 
	{
		// TODO Auto-generated method stub
		/*
		 * SerializeHelper demo = new SerializeHelper(); List<String> list = new
		 * ArrayList<String>(); list.add("1"); String filePath = "/tmp/employee.ser";
		 * demo.SaveToFile(list,filePath); list =(List<String>)
		 * demo.DeSerializeFromFile(filePath );
		 */
		MainForm frm = new MainForm();
		frm.ShowForm();
		
	}
}

